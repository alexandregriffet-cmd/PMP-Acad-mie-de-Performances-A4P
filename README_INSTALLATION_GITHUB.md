Installation GitHub Pages
1. Crée un nouveau dépôt GitHub, par exemple `pmp-a4p-autonome`.
2. Dépose `index.html` à la racine du dépôt.
3. Active GitHub Pages sur `main / root`.
4. Ouvre l’URL publiée.

Cette version est autonome : un seul fichier HTML suffit.
